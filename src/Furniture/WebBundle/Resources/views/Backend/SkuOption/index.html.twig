{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/SkuOption:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.assortment'|trans }}</li>
    <li>Sku attribute</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('furniture_backend_sku_option_create'), 'Create attribute') }}
    </div>
    <h1><i class="glyphicon glyphicon-list-alt"></i> {{ 'sylius.product_attribute.index_header'|trans|raw }}</h1>
</div>

{{ pagination(sku_options) }}
{{ list(sku_options) }}
{{ pagination(sku_options) }}

{% endblock %}
