<div class="swiper-container" data-autoplay="0" data-loop="0" data-speed="500" data-center="0" data-slides-per-view="responsive" data-xs-slides="2" data-int-slides="2" data-sm-slides="3" data-md-slides="4" data-lg-slides="5" data-add-slides="5">
    <div class="swiper-wrapper">
        {% for product in products %}
            {% include 'FrontendBundle:Catalog/products:item.html.twig' with {'product':product} %}
    {#        <div class="swiper-slide">
                <div class="paddings-container">
                    <div class="product-slide-entry">
                        <div class="product-image">
                            {% if product.getImage() %}
                                <img src="{{ product.image.path|imagine_filter('s200x250') }}" alt="" />
                            {% endif %}
                            <div class="bottom-line">
                                <a class="bottom-line-a" href="{{ path('product', {'product':product.id}) }}" ><i class="fa fa-try"></i> {{ 'frontend.detailed' | trans }}</a>
                            </div>
                        </div>
                        <a class="title" href="{{ path('product', {'product':product.id}) }}">{{ product.name }}</a>
                        <div class="article-container style-1">
                            <p>{{ product.shortDescription }}</p>
                        </div>
                        <div class="price">
                            <div class="current">
                                {% if is_granted('VIEW_PRICE', product) %}
                                    {{ product|product_price|money }}
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </div> #}
        {% endfor %}
    </div>
    <div class="pagination"></div>
</div>
