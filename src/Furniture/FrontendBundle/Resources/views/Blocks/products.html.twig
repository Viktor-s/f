{% from 'FrontendBundle:Macros:products.html.twig' import product_grid_view %}
<div class="swiper-container" data-autoplay="0" data-loop="0" data-speed="500" data-center="0" data-slides-per-view="responsive" data-xs-slides="2" data-int-slides="4" data-sm-slides="4" data-md-slides="4" data-lg-slides="4" data-add-slides="5">
    <div class="swiper-wrapper">
        {% for product in products %}
            {{ product_grid_view(product, 3) }}
        {% endfor %}
    </div>
    <div class="pagination"></div>
</div>
