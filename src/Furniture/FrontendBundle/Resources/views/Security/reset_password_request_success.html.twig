{% extends 'FrontendBundle:Security:layout.html.twig' %}

{% block content %}
    <div class="col-md-6 information-entry col-md-offset-3">
        <div class="login-box">
            <div class="article-container style-1 text-center"><h3>{{ 'frontend.reset_password'|trans }}</h3></div>
            <p class="lead text-center">{{ 'frontend.request_reset_password_successfully'|trans }}</p>
            <p class="text-center"><a href="{{ path('security_reset_password_request_reset') }}">{{ 'frontend.send_reset_password'|trans }}</a></p>
            <p class="lead"></p>
            {% for type in ['success', 'error'] %}
                {% for flash in app.session.flashbag.get(type) %}
                    <div class="alert alert-{{ type == 'error' ? 'danger' : type }}">
                        <a class="close" data-dismiss="alert" href="#">×</a>
                        {{ flash }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
    </div>
{% endblock %}