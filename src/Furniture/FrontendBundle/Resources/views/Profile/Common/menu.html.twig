{% block root %}
{% spaceless %}
    <ul>
        {% for child in item.children %}
            <li>
                <a href="{{ child.uri }}">{{ child.label }}</a>
            </li>
        {% endfor %}
    </ul>
{% endspaceless %}
{% endblock %}